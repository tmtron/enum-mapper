package com.test;

// comment"

import com.tmtron.enums.MapAllEnums;

@MapAllEnums(enums = LauncherAge_Source.LauncherAge.class)
public class LauncherAge_Source {
    enum LauncherAge {ONLINE, OLD, OFFLINE}
}